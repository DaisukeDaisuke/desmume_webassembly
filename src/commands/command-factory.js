import { createScriptCommands } from "./script-commands.js";
import { createInputCommands } from "./input-commands.js";
import { createRuntimeCommands } from "./runtime-commands.js";
import { createRomCommands } from "./rom-commands.js";
import { createSaveCommands } from "./save-commands.js";
import { createStateCommands } from "./state-commands.js";
import { createRecentFileCommands } from "./recent-file-commands.js";
import { createScreenshotCommands } from "./screenshot-commands.js";
import { createMemoryCommands } from "./memory-commands.js";
import { createDisassemblyCommands } from "./disassembly-commands.js";
import { createDebuggerControlCommands } from "./debugger-control-commands.js";
import { createContextCommands } from "./context-commands.js";
import { createUtilityCommands } from "./utility-commands.js";
import { createFeatureCommands } from "./feature-commands.js";

export function createCommands(context) {
    const {
        ANALYSIS_BASELINE_SLOT_PREFIX,
        ANALYSIS_BASELINE_STATE_FORMAT_VERSION,
        analysisBaselineSlotToken,
        applyFreezes,
        applyScaleRotation,
        applySaveAndReloadRom,
        bigEndianValue,
        blockSaveFlush,
        bootWaitMs,
        breakpointOwners,
        bytesFromFlexibleParams,
        bytesFromParams,
        cancelAndWait,
        cancelOperation,
        copyText,
        currentRomIdentity,
        dispatchScriptEvent,
        download,
        drawFrame,
        drawLoadedStateFrame,
        emulatorActivity,
        ensureReady,
        ensureRomLoaded,
        ensureWasmReady,
        frameService,
        fileTransactionService,
        formatDisassemblyText,
        getPc,
        getRegisters,
        hasLoadedRom,
        hex,
        idbGet,
        idbPut,
        instructionWidthForMode,
        isAnalysisBaselineSlot,
        loadStateBytesFromMemory,
        log,
        matchSearchCondition,
        memorySearchRangeKey,
        memorySearchRanges,
        modeNumber,
        native,
        onScreenValid,
        opcodeWordsFromInput,
        openPicker,
        parseAddress,
        parseNumber,
        pauseForFileLoad,
        publicCallStackData,
        publicOtherCoroutines,
        pumpAudio,
        queueAutoUpdateLoop,
        readAnalysisBaseline,
        readCallStackData,
        readFileFromInput,
        readSized,
        requireValidScreen,
        recordRecentFile,
        refreshDebuggerViews,
        reloadCurrentRom,
        rememberSlot,
        renderBreakpoints,
        renderCallStack,
        renderFreezes,
        renderHotkey,
        renderMemoryDump,
        renderRecentFiles,
        renderRegisters,
        renderScriptConsole,
        restoreAfterFileLoad,
        runCommand,
        runDebuggerInstruction,
        runIsolatedScript,
        runSandboxBoundarySelfTest,
        runTraceStepper,
        runUntilNextBranchOrReturn,
        runUntilTrueNextBranch,
        saveKeymap,
        scriptSummary,
        setKey,
        setTouchState,
        sha256Hex,
        shouldIncludeDisassemblyBytes,
        snapshotContext,
        splitBinaryBits,
        startPersistentScript,
        state,
        stopAfterFailedStateLoad,
        stopAutoUpdateLoop,
        stopPersistentScript,
        swap16,
        swap32,
        syncNativeBreakStatus,
        tick,
        toButtonList,
        u16FromBytes,
        u32FromBytes,
        ui,
        updateStatus,
        waitChecked,
        writeAnalysisBaseline,
        writeRomFile
    } = context;

    const commands = {
        ...createContextCommands({
            ANALYSIS_BASELINE_SLOT_PREFIX,
            analysisBaselineSlotToken,
            call: (name, params) => commands[name](params),
            currentRomIdentity,
            emulatorActivity,
            ensureRomLoaded,
            fileTransactionService,
            hasLoadedRom,
            idbGet,
            native,
            readAnalysisBaseline,
            sha256Hex,
            snapshotContext,
            state,
            syncNativeBreakStatus,
            ui,
            writeAnalysisBaseline
        }),
        ...createRomCommands({
            state,
            ui,
            native,
            cancelAndWait,
            fileTransactionService,
            ensureReady,
            ensureWasmReady,
            bytesFromParams,
            readFileFromInput,
            openPicker,
            pauseForFileLoad,
            restoreAfterFileLoad,
            stopAfterFailedLoad: stopAfterFailedStateLoad,
            writeRomFile,
            reloadCurrentRom,
            bootWaitMs,
            log
        }),
        ...createSaveCommands({
            ui,
            native,
            cancelAndWait,
            fileTransactionService,
            ensureReady,
            ensureRomLoaded,
            readFileFromInput,
            openPicker,
            pauseForFileLoad,
            restoreAfterFileLoad,
            stopAfterFailedLoad: stopAfterFailedStateLoad,
            applySaveAndReloadRom,
            bootWaitMs,
            rememberSlot,
            idbGet,
            idbPut,
            recordRecentFile,
            download,
            log
        }),
        ...createStateCommands({
            analysisBaselineSlotToken,
            blockSaveFlush,
            bytesFromParams,
            cancelAndWait,
            dispatchScriptEvent,
            download,
            drawLoadedStateFrame,
            ensureReady,
            ensureRomLoaded,
            idbGet,
            idbPut,
            isAnalysisBaselineSlot,
            loadStateBytesFromMemory,
            log,
            native,
            fileTransactionService,
            openPicker,
            pauseForFileLoad,
            readFileFromInput,
            recordRecentFile,
            rememberSlot,
            restoreAfterFileLoad,
            state,
            stopAfterFailedStateLoad,
            ui
        }),
        ...createRecentFileCommands({
            applySaveAndReloadRom,
            blockSaveFlush,
            bootWaitMs,
            cancelAndWait,
            fileTransactionService,
            drawLoadedStateFrame,
            ensureReady,
            ensureRomLoaded,
            idbGet,
            loadStateBytesFromMemory,
            native,
            pauseForFileLoad,
            rememberSlot,
            renderRecentFiles,
            restoreAfterFileLoad,
            state,
            stopAfterFailedStateLoad,
            ui
        }),
        ...createScreenshotCommands({
            requireValidScreen,
            state,
            ui
        }),
        ...createRuntimeCommands({
            applyFreezes,
            applyScaleRotation,
            bootWaitMs,
            cancelAndWait,
            cancelOperation,
            fileTransactionService,
            dispatchScriptEvent,
            drawFrame,
            ensureReady,
            ensureRomLoaded,
            frameService,
            hasLoadedRom,
            native,
            onScreenValid,
            pauseForFileLoad,
            pumpAudio,
            queueAutoUpdateLoop,
            reloadCurrentRom,
            restoreAfterFileLoad,
            state,
            stopAfterFailedLoad: stopAfterFailedStateLoad,
            stopAutoUpdateLoop,
            syncNativeBreakStatus,
            ui,
            updateStatus
        }),
        ...createInputCommands({
            state,
            ensureRomLoaded,
            renderHotkey,
            saveKeymap,
            setKey,
            setTouchState,
            toButtonList,
            waitChecked
        }),
        ...createDisassemblyCommands({
            bytesFromFlexibleParams,
            ensureRomLoaded,
            ensureWasmReady,
            formatDisassemblyText,
            getPc,
            getRegisters,
            instructionWidthForMode,
            modeNumber,
            native,
            opcodeWordsFromInput,
            parseAddress,
            parseNumber,
            shouldIncludeDisassemblyBytes,
            u16FromBytes,
            u32FromBytes,
            ui
        }),
        ...createUtilityCommands({
            ensureWasmReady,
            native,
            splitBinaryBits
        }),
        ...createMemoryCommands({
            applyFreezes,
            bigEndianValue,
            bytesFromFlexibleParams,
            ensureRomLoaded,
            hex,
            log,
            matchSearchCondition,
            memorySearchRangeKey,
            memorySearchRanges,
            native,
            openPicker,
            parseAddress,
            parseNumber,
            readFileFromInput,
            readSized,
            renderFreezes,
            renderMemoryDump,
            state,
            swap16,
            swap32,
            ui
        }),
        ...createDebuggerControlCommands({
            breakpointOwners,
            copyText,
            ensureReady,
            ensureRomLoaded,
            hex,
            log,
            native,
            parseAddress,
            publicCallStackData,
            publicOtherCoroutines,
            readCallStackData,
            renderBreakpoints,
            renderCallStack,
            resume: (params) => commands.resume(params),
            runDebuggerInstruction,
            runTraceStepper,
            runUntilNextBranchOrReturn,
            runUntilTrueNextBranch,
            state,
            status: (params) => commands.status(params),
            ui,
            updateStatus
        }),
        ...createScriptCommands({
            state,
            ui,
            startPersistentScript,
            stopPersistentScript,
            scriptSummary,
            renderScriptConsole,
            runSandboxBoundarySelfTest,
            runIsolatedScript,
            runCommand
        }),
        ...createFeatureCommands({ native, ui }),
    };
    
    Object.assign(commands, {
        reg: commands.memoryGetRegister,
        regw: commands.memorySetRegister,
        read8: commands.memoryReadByte,
        read16: commands.memoryReadWord,
        read32: commands.memoryReadDword,
        write8: commands.memoryWriteByte,
        write16: commands.memoryWriteWord,
        write32: commands.memoryWriteDword
    });
    return commands;
}
